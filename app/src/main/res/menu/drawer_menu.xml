<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:id="@+id/menu_map_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/menu_satellite_item"
            android:icon="@drawable/ic_satellite"
            android:title="@string/menu_satellite_title"/>
        <item
            android:id="@+id/menu_terrain_item"
            android:icon="@drawable/ic_terrain"
            android:title="@string/menu_terrain_title"/>
    </group>

    <group
        android:id="@+id/menu_other_group"
        android:checkableBehavior="none">
        <item
            android:id="@+id/menu_location_item"
            android:icon="@drawable/ic_location"
            android:title="@string/menu_location_title"/>
    </group>

</menu>